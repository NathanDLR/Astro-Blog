---
import BaseLayout from "../layouts/BaseLayout.astro";
const pageTitle = "Home Page";
---

<BaseLayout pageTitle={pageTitle}> <!-- BaseLayout is the child component wrapped by its parent index.astro -->
	<!-- This is the content that'll be injected in the child replacing the <slot /> tag -->
</BaseLayout>